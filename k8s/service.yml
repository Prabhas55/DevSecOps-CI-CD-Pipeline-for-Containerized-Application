apiVersion: v1
kind: Service
metadata:
  name: tetris-service
  namespace: default
  labels:
    app: tetris
spec:
  type: LoadBalancer
  selector:
    app: tetris
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31870
